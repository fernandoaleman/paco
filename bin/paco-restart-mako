#!/usr/bin/env bash
# paco:summary=Reload mako config (via makoctl, keeps notifications in flight)

set -euo pipefail

if pgrep -x mako > /dev/null; then
  makoctl reload
else
  # Not running — start it
  setsid mako > /dev/null 2>&1 &
  disown 2> /dev/null || true
fi
