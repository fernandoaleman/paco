#!/usr/bin/env bash
# paco:summary=Check if a pacman package is installed (exit 0 if present)

set -euo pipefail

if [[ $# -ne 1 ]]; then
  echo "Usage: paco-pkg-present <pkg>" >&2
  exit 2
fi

pacman -Qi "$1" > /dev/null 2>&1
