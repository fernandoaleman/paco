# paco-shipped containers.conf override. Loaded after the main
# /etc/containers/containers.conf via the .d/ override mechanism.
#
# Silences the "Executing external compose provider" notice that podman
# emits every time it dispatches to podman-compose.

[engine]
compose_warning_logs = false
